tilde — Session 1 Progress (Initializer Agent)
================================================

Date: 2025-04-22

## Completed

1. **feature_list.json** — 260 test cases (232 functional, 28 style)
   - 27 tests with 10+ steps each
   - Covers all features from app_spec.txt exhaustively
   - All tests start with passes: false

2. **init.sh** — Development environment setup script
   - Loads .env (errors if missing with .env.example present)
   - Checks system dependencies (rustc, cargo, sqlite3, exiftool, ffmpeg)
   - Runs cargo build
   - Creates development data directories
   - Prints helpful summary

3. **Cargo workspace** — All 13 crates compile successfully
   - tilde-core (config, auth, database, migrations, error types)
   - tilde-server (axum binary entry point)
   - tilde-cli (clap CLI with all subcommands stubbed)
   - tilde-dav, tilde-cal, tilde-card, tilde-photos, tilde-notes
   - tilde-collections, tilde-email, tilde-mcp, tilde-backup, tilde-notify

4. **Project structure**
   - migrations/001_initial.sql — Complete database schema
   - locales/en.ftl — English locale strings
   - assets/login.html — Nextcloud Login Flow v2 page
   - README.md — Project overview and setup instructions
   - .gitignore — Proper exclusions

5. **CLI structure** — All subcommands defined via clap derive macros
   - init, serve, status, diagnose
   - auth (reset-password, app-password, session)
   - mcp (token, audit)
   - notes (search, list)
   - photos (list, tag, reindex, thumbnail)
   - calendar (list, events), contacts (list, search)
   - collection (create, list)
   - email (search, thread, show, reindex, status)
   - backup (status, now, list, verify, pin)
   - notifications (test, list, config)
   - export, import, reindex, update (check, download)
   - Global flags: --config, --json, --dry-run, --verbose

## Next Steps (for future sessions)

1. Implement tilde-core: config loading via figment, SQLite init, migration runner
2. Implement auth: Argon2id password hashing, session tokens, app-passwords
3. Build axum server scaffolding with tower middleware
4. Implement /health and /metrics endpoints
5. Implement WebDAV Class 1 handler in tilde-dav
6. Begin CalDAV/CardDAV integration via RustiCal
